{% extends 'base.html.twig' %}

{% block title %}Inscription / connexion{% endblock %}

{% block body %}
    <account-log-in-sign-up form-register="{{ registrationForm }}" lastusername="{{ last_username }}" error="{{ error }}" token="{{ csrf_token('authenticate') }}" email="{{ email }}" password="{{ password }}"></account-log-in-sign-up>
{% endblock %}
